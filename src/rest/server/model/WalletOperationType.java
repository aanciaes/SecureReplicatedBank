package rest.server.model;

/**
 * Wallet server operation types
 */
public enum WalletOperationType {
    GET_ALL,
    GENERATE_MONEY,
    TRANSFER_MONEY,
    GET_BALANCE,
    SUM,
    SET_BALANCE,
    GET_BETWEEN
}
